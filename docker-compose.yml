services:
  asismetro-automations:
    image: ghcr.io/ismola/asismetro-automations:main
    container_name: asismetro-automations
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      TZ: ${TZ:-Europe/Madrid}
      STAGE: ${STAGE:-production}
      VALID_TOKEN: ${ASISMETRO_BEARER_TOKEN:-sample}
      AUTO_DELETE_LOGS: ${AUTO_DELETE_LOGS:-True}
      HEADLESS_MODE: ${HEADLESS_MODE:-auto}
      BROWSER_LANGUAGE: ${BROWSER_LANGUAGE:-en}
      DOWNLOAD_DIR: ${DOWNLOAD_DIR:-temp_downloads}
      PAGE_MAX_TIMEOUT: ${PAGE_MAX_TIMEOUT:-7}
      DOWNLOAD_MAX_TIMEOUT: ${DOWNLOAD_MAX_TIMEOUT:-4}
      BASE_URL: ${BASE_URL:-https://www.google.com/}
    healthcheck:
      test: [ "CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:3000/')" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: calendar_subscription_hub
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/calendar_subscription_hub?schema=public
      DIRECT_DATABASE_URL: postgresql://postgres:postgres@postgres:5432/calendar_subscription_hub?schema=public
    command: sh -c "npm ci && npx prisma migrate deploy"
    depends_on:
      postgres:
        condition: service_healthy

  web:
    build:
      context: .
      target: runner
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
      DATABASE_URL: ${DATABASE_URL_DOCKER:-postgresql://postgres:postgres@postgres:5432/calendar_subscription_hub?schema=public}
      REDIS_URL: ${REDIS_URL_DOCKER:-redis://redis:6379}
      APP_BASE_URL: ${APP_BASE_URL}
      ASISMETRO_API_BASE_URL: ${ASISMETRO_API_BASE_URL_DOCKER:-http://asismetro-automations:3000}
      APP_ENCRYPTION_KEY: ${APP_ENCRYPTION_KEY:-0123456789abcdef0123456789abcdef}
      SESSION_SECRET: ${SESSION_SECRET:-dev-session-secret}
      ASISMETRO_BEARER_TOKEN: ${ASISMETRO_BEARER_TOKEN:-sample}
      DEFAULT_REFRESH_MINUTES: ${DEFAULT_REFRESH_MINUTES:-60}
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      asismetro-automations:
        condition: service_healthy

  worker:
    build:
      context: .
      target: worker
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL_DOCKER:-postgresql://postgres:postgres@postgres:5432/calendar_subscription_hub?schema=public}
      REDIS_URL: ${REDIS_URL_DOCKER:-redis://redis:6379}
      APP_BASE_URL: ${APP_BASE_URL}
      ASISMETRO_API_BASE_URL: ${ASISMETRO_API_BASE_URL_DOCKER:-http://asismetro-automations:3000}
      APP_ENCRYPTION_KEY: ${APP_ENCRYPTION_KEY:-0123456789abcdef0123456789abcdef}
      SESSION_SECRET: ${SESSION_SECRET:-dev-session-secret}
      ASISMETRO_BEARER_TOKEN: ${ASISMETRO_BEARER_TOKEN:-sample}
      DEFAULT_REFRESH_MINUTES: ${DEFAULT_REFRESH_MINUTES:-60}
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      asismetro-automations:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
